[project]
name = "local-mcp-weather"
version = "0.1.0"
description = "A local MCP weather server using the NWS API"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.27.0",
]
